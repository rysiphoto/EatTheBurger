<li>
	{{name}}

	<button class="change-eat" data-id="{{id}}" data-neweat="{{eat}}">
		{{#if eat}}TIME TO EAT!{{else}}YOU ATE ALL THAT?!{{/if}}
	</button>
	<button class="delete is-small" data-id="{{id}}">Delete Burger</button>
</li>